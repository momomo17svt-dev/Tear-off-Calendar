# プロジェクト進捗管理 (PROGRESS.md)

このファイルは AIアシスタント（Claude Code / Antigravity） が自律的に読み込み、更新することでプロジェクトの進捗をトラッキングします。
各タスクが完了し、Pull Requestが作成された段階で、AI自身がこのファイルの `[ ]` を `[x]` に変更してください。

## フェーズ 1: プロジェクトの初期設定
- [x] Expoプロジェクトの初期化（`create-expo-app` または指定のボイラープレート）
- [x] 必要なライブラリのインストール（`expo-sqlite`, `zustand`, `expo-file-system`, ナビゲーション関連等）
- [x] フォルダ構成のセットアップ（`src/components`, `src/screens`, `src/store`, `src/db` 等）

## フェーズ 2: データベースと状態管理の構築
- [x] SQLite初期化処理の実装（DB接続設定）
- [x] `events` テーブルと `settings` テーブルの作成（スキーマ定義）
- [x] データベース操作用のユーティリティ関数（CRUD）作成
- [x] Zustand（または Context）を用いたグローバルステートの構築

## フェーズ 3: UI/UXとナビゲーションの実装
- [x] Bottom Tab Navigation の基礎組み込み（背景透過・半透明デザインの適用）
- [x] 【ホームタブ】日めくり画面のUI実装（物理的な日めくりカレンダー風、紙・穴・日付エリア）
- [x] 【ホームタブ】予定・誕生日のリスト表示と、スワイプでの日付切り替えロジック
- [x] 【ホームタブ】背景画像の複数アップロード・固定/ランダムモード切り替え
- [x] 【ホームタブ】リアルな「紙をめくる・戻す」アニメーション（指の動きと完全連動、1フレームのチラつきなし）
- [x] 【追加タブ】モーダル式の下からせり上がる入力フォーム（タイトル、日付、カテゴリの登録）
- [x] 【追加タブ】入力内容をSQLiteに保存し、ホーム画面のステートを更新する処理
- [x] 【設定タブ】背景テーマ選択（グレー/コルク/木目）カスタムドロップダウン実装
- [x] 【設定タブ】背景画像のON/OFFトグルスイッチ実装
- [x] 【設定タブ】端末から画像を選択し、`expo-file-system` で保存、URIをSQLiteに保存する処理

## フェーズ 4: アプリのブラッシュアップと仕上げ
- [x] アプリ全体の透過UI・グラスモーフィズムデザインの微調整（余白、色味、フォント）
- [x] エッジケースのテスト（画像が未設定の場合のフォールバック、予定が空の場合の表示など）
- [x] GitHubへの初期デプロイと CI/CD（GitHub Actions/Codemagic）連携用設定ファイルの作成

> **AIアシスタント への指示:** 
> 一つのチェック項目が完了し、ユーザーの承認（バイブチェック完了）とPR作成が終わったら、ここを `[x]` に更新して保存してください。

## フェーズ 5: イベント管理強化・カレンダー連携（旧アーキテクチャ）
- [x] 予定が多い場合のスクロール対応・折りたたみUI
- [x] 予定タップで編集・削除・エクスポートのアクションシート（iOS ActionSheetIOS / Android カスタムシート）
- [x] 予定追加モーダルをカレンダーピッカー式日付入力に変更
- [x] 予定の編集機能
- [x] 毎年繰り返しトグル
- [x] `expo-calendar` を使った端末ネイティブカレンダーへのエクスポート機能

## フェーズ 6: ネイティブカレンダー単一ソース化（アーキテクチャ刷新）
- [x] SQLite `events` テーブルへの依存を完全に削除（データソースの一本化）
- [x] `nativeCalendarStore` (Zustand) の実装：ネイティブカレンダーからのフェッチとキャッシュ管理
- [x] アプリ起動時の自動キャッシュフェッチ（過去3ヶ月 〜 未来12ヶ月）の実装
- [x] ホーム画面 (`index.tsx`) のデータソースを新ストアへ切り替え
- [x] モーダル (`modal.tsx`) の保存・更新・削除をネイティブカレンダーへの直接操作に修正
- [x] 設定画面 (`settings.tsx`) に「表示するカレンダーの選択」UIを追加
- [x] SQLite `settings` テーブルに `selected_calendar_ids` の永続化処理を追加
- [x] 予定追加・編集をネイティブカレンダーUIに切り替え（`createEventInCalendarAsync` / `editEventInCalendarAsync`）
- [x] 複数日にまたがるイベントの全日程表示（フォールアウトループ）
- [x] アプリ外で削除された予定の整合性対応（`purgeStaleEvent` + `useFocusEffect`）
- [x] 設定画面のサムネイル縦横比をホーム画面と一致させる修正

## フェーズ 7: UIテーマ拡張とカレンダー画面の実装
- [x] UIテーマのバリエーション拡充（和紙、桜、抹茶、藍染、紅葉などの和風テーマ追加）
- [x] 【カレンダータブ】カスタムカレンダー画面の実装（月次切り替え、六曜表示ロジックの組み込み）
- [x] 【カレンダータブ】日付タップでホーム画面の該当日にジャンプする機能
- [x] EAS Build (Expo Application Services) の設定最適化（bundleIdentifier, projectId 等の修正）
- [x] iOS での EAS Build エラー解消とビルド環境の安定化
- [x] カレンダー画面のパフォーマンス最適化（イベントが多い日のレンダリング負荷軽減）
- [x] ダークモード対応の検討と配色定義

## フェーズ 8: パフォーマンス・コード品質改善
- [x] 背景画像の削除時のストレージリーク解消
- [x] Lint警告と未使用変数のクリーンアップ
- [x] 【カレンダータブ】予定が多い場合のカレンダー見切れ問題の解消

## フェーズ 9: 日記機能と UI/レイアウト改善
- [x] 【日記タブ】タブバーに 5 つ目のタブを追加（追加と設定の間、`book.fill` アイコン）
- [x] 【日記タブ】`diaries` テーブル（id/date/title/content/tags/image_uris/timestamps）の追加と DAL 実装
- [x] 【日記タブ】Zustand `diaryStore` でメモリキャッシュ（`diariesByDate`）と CRUD/検索を提供
- [x] 【日記タブ】複数画像添付（`documentDirectory/diaries/` 配下にローカル保存、URI のみ DB 保持）
- [x] 【日記タブ】タイトル / タグでの LIKE 検索（メタ文字エスケープ済み）
- [x] 【日記タブ】ホーム画面の選択日付と連動（`navigationStore.selectedDate` を追加）
- [x] 【日記タブ】日付前後ボタン・日付ピッカー・「今日へ」リンクで日付変更
- [x] 【広告】AdBanner を絶対配置 → 画面下部のレイアウトに組み込み、コンテンツとの被りを解消
- [x] 【広告】`settings.is_premium` キーを追加し、購入済みなら AdBanner が `null` を返す
- [x] 【ホーム/カレンダー】iPad で前日/前月カードがチラ見えする問題を解消（`FLY_OUT_DISTANCE = SCREEN_HEIGHT + 100`）
- [x] 【ホーム/カレンダー】カード寸法を `src/constants/cardLayout.ts` に共通化し、タブ切替時の上端ジャンプを解消
